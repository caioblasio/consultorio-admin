import React from 'react'
import NavLink from 'containers/NavLink'

const HeaderNavLink = ({ ...rest }) => (
  <NavLink color="grey.dark" variant="button" {...rest} />
)

export default HeaderNavLink
