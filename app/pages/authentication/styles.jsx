import { styled } from '@mui/system'
import { Container } from '@mui/material'

export const StyledAuthBox = styled(Container)({
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
  minHeight: '100vh',
  textAlign: 'center',
})
